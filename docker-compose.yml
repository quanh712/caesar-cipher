services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL-http://localhost:8080}
        VITE_USE_MOCK_API: ${VITE_USE_MOCK_API:-true}
    ports:
      - "${FRONTEND_PORT:-8081}:80"
    restart: unless-stopped
